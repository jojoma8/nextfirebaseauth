import { createContext } from "react";

export const EditPostContext = createContext({});
export const DeletePostContext = createContext({});
export const PassUserIDContext = createContext({});
export const PassDocIDContext = createContext({});
